{# Loop all the zones #}
<div>
  testinbgs 1
</div>
{% for zone, modules in agilitypage.zones %}
<div>
  testinbgs {{ zone }}
</div>
	{# Only render the current zone name #}
	{% if zone === zoneName %}
		{% for module in  modules  %}
    <div>
      testinbgs {{ module.module }}
    </div>
			{# Render the module based on the module reference name  -ignoring missing module files... #}
			{% include  "./agility-pageModules/" + module.module.toLowerCase() + ".njk" %}
		{% endfor %}
	{% endif %}
{% endfor %}